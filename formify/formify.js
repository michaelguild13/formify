var formify = function () {
  "use strict";
  // find formify
  // build virtual form for submit
  // create formify form
  // create copy of formify model

  var form = document.getElementById('formify');

  function init () {
  }

  function initInputs ( form ) {

  }

  function createInput ( el, className ) {

  }

  init();
};
